# Internationalization (i18n)

**Rule:** User-visible UI strings must not be hard-coded. Use `t(locale, key)` / `createT(locale)`.

**Catalog fallback is English (`en`).** Optional locales: `zh` (Simplified Chinese), `zh-TW` (Traditional Chinese). All three catalogs must share the same keys (`messages.test.ts` enforces this).

**User preference default is `zh`** (Simplified Chinese). Users can still lock `system` / `en` / `zh-TW` in Settings. Unknown OS languages still fall back to English when preference is `system`.

## Source locations

| Path | Role |
|------|------|
| `src/i18n/messages.ts` | Catalog: **`en` is the key authority**; `zh` must cover every key |
| `src/i18n/zh-tw.ts` | Traditional Chinese (Taiwan) catalog |
| `src/i18n/index.ts` | `t` / `createT` / `resolveLocale` / `resolveLocaleFromSystem` / types |
| Components | Reference keys only — never hard-code `"Settings"` / `"设置"` |
| `src-tauri/src/tray_i18n.rs` | Native tray strings (keep in sync with `tray.*` keys; reads `settings.locale`; `"system"` → OS lang) |

## Adding or changing copy

1. Add the key to the **`en` object** in `messages.ts` first (dot keys: `area.action`).
2. **Same PR:** fill `zh` and `zh-tw.ts` with complete translations.
3. In components: `const tr = useMemo(() => createT(locale), [locale]);` then `tr("sidebar.settings")`.
4. Variables: template `{name}`, call `tr("project.trustFirst", { name: proj.name })`.
5. Do **not** spread `locale === "zh" ? "…" : "…"`; replace leftovers with keys.
6. Product / model names (`Grok 4.5`, `Agent`) may stay English; descriptive UI must be translated.
7. Language switch writes `settings.locale` and updates UI immediately.
8. Prefer `createT(locale)` over partial label maps (missing keys show raw `settings.foo`).

## Defaults

| Layer | Default |
|-------|---------|
| `AppSettings.locale` | `"zh"` (Simplified Chinese; users may lock `system` / `en` / `zh-TW`) |
| `resolveLocale(undefined)` | `zh` |
| `resolveLocale("system")` | maps Host OS tag / `navigator.language` → `en` \| `zh` \| `zh-TW` |
| Tray unknown locale | English |
| Settings language dropdown | **System** first, then English / 简体 / 繁體 |
| Compact counts (`formatTokenCount` / `formatLocaleCount`) | `en` → K/M/B (`500K`); `zh` → 万/千/百; `zh-TW` → 萬/億. Never hard-code 万 on the English path. |

## Follow system

- Preference value stored in `settings.locale` is the string `"system"` (not the resolved catalog id).
- Frontend: `resolveLocaleFromSystem(langTag)` (pure) + Host `__GROK_BOOT_OS_LANG__` + `navigator.languages` + `languagechange` while preference is system.
- Tray / boot: `Locale::from_system` prefers macOS `AppleLanguages` / Windows UI LANGID, then POSIX `LANG` / `LC_*` (ignores `C` / `POSIX`).
- One-shot: persisted factory `"en"` → `"system"` (`localeFollowSystemMigrated`). Explicit `zh` / `zh-TW` / `system` stay.

## Translation quality

- **English:** primary product language for open source; clear, concise desktop tone.
- **Chinese:** complete sentences, consistent product tone; optional for users who switch locale.
